/* package july8;
import java.util.Scanner;
public class Test {
    public static void main(String[] args){

        Scanner sc = new Scanner(System.in);
        System.out.println("Enter your Input");

        // To scan the next input in integer then this will work
        int input = sc.nextInt();

        // To scan in a float value then use a float instead of int

        float input = sc.nextFloat();

        // To do the same for the string than
         String input =sc.next(); //( reads only until the break and then consider the break as the end point)
        // use thsi instead
        String input =sc.nextLine();


*/



